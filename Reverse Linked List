// O(n) time | O(1) space
var reverseList = function(head) {
    let prev = null;
    let curr = head;

    while (curr !== null) {
        next = curr.next;
        curr.next = prev;
        prev = curr;
        curr = next;
    }

    return prev
};
